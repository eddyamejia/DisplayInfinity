/*
 *
 * Locale constants
 *
 */

export const DEFAULT_LOCALE = 'en';

const scope = 'Locale';

export const INIT = `${scope}/INIT`;
export const CHANGE = `${scope}/CHANGE`;

export const CACHE = {
  DATA: `${scope}/CACHE/DATA`,
};
