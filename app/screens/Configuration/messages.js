import { defineMessages } from 'react-intl';

const scope = 'ConfigurationScreen';

const messages = defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: 'Configuration',
  },
});

export default messages;
