/*
 *
 * {{ properCase name }} Messages
 *
 */

import { defineMessages } from 'react-intl';

const scope = '{{ properCase name }}';

const messages = defineMessages({
  title: {
    id: `${scope}.title`,
    defaultMessage: `This is the ${scope} component !`,
  },
});

export default messages;
